{
  "name": "lyfstyl-functions",
  "private": true,
  "type": "module",
  "engines": { "node": "^18" },
  "scripts": {
    "lint": "eslint .",
    "build": "echo 'No build step for plain ESM'",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
